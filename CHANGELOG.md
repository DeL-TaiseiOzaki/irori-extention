# Change Log

## Unreleased（公開されません）

> 2026-09-18 に開発終了．以下の変更はリポジトリには入っているが，Marketplace へは
> 公開しない．層モデルはデスクトップアプリ irori が実装する．経緯は
> [docs/DESIGN.md](docs/DESIGN.md) の Key Decisions を参照．

### 破壊的変更

- **拡張の名称と識別子を irori に統一した．** リポジトリ名は `irori-extention`，拡張 ID は `del-taiseiozaki.irori-extention`，表示名は `irori for VS Code`．設定・コマンド・ビューの ID 接頭辞が `layeredkb.*` から `irori.*` に変わる（`layeredkb.layers` → `irori.layers` など）．VS Code は旧 ID の設定値を引き継がないため，既存の利用者は設定を新しいキーへ書き直す必要がある．旧 ID を受け付ける互換レイヤーは用意していない．

## 0.2.0

### 破壊的変更

- **レイヤー分類がスコープ単位になった．** git submodule として取り込んだリポジトリは，それ自体が 3 層を内包する「スコープルート」として扱われる．各ファイルはまず所属スコープに分けられ，そのスコープのフレーム内で層が決まる．
- **既定のスキーマ層がスコープルート直下に固定された．** `**/CLAUDE.md` のような無制限のパターンをやめ，`CLAUDE.md` `CLAUDE.local.md` `AGENTS.md` `GEMINI.md` `.claude/**` などはスコープルート直下のみを指す．submodule の `CLAUDE.md` が親のスキーマ層へ吸い上げられなくなり，そのスコープ自身のスキーマ層に入る．同時に，`.claude/rules/*.md` がナレッジベース層へ落ちて同じスコープのスキーマが 2 つの層に割れる問題も解消した．スコープルート直下でない `CLAUDE.md` `AGENTS.md` 等（例: `docs/CLAUDE.md`）は，スキーマ層ではなくナレッジベース層に入る．
- **`contents/` 配下はファイル種別で分類されなくなった．** 交換面はファイルの種類ではなく「系の外と交換する面である」ことで定義されるため，配下は種別を問わず交換面のレイヤーにまとまる．Google Drive のマウント内の `.md` がナレッジベース層に，`.csv` がオントロジー層に散る問題が解消した．
- **既定の Raw データ層が `roots: []` を持たなくなり，通常のワークスペース内レイヤーになった．** `patterns` は `contents/**` `raw/**` `data/**` `attachments/**`．既定設定の利用者は，これらの配下のファイルが「その他」から「Raw データ層」へ移動する．`roots` を明示設定している利用者への影響は無い．

いずれも分類の再計算だけで，ファイルの移動や書き換えは発生しない．従来の挙動は `irori.layers` 設定で復元できる．

### 追加

- `contents/` 直下の各エントリをマウント点として認識し，3 状態を区別する．`attached`（マウント済み）・`not attached`（この端末では中身を読めない）・`local only`（マウントであるべき場所に実ローカルディレクトリがある）．判定はシンボリックリンクかどうかではなくデバイスの違いによるので，実 FUSE マウントや Windows の junction も正しく扱える．
- `local only` はマウント失敗の兆候として警告する．`contents/` は Git 管理外なので，そこに溜まったファイルは 1 台のマシンにしか存在せず，どこにもバックアップされない．
- 分類結果の各ファイルが所属スコープ（id・ラベル・種別）を持つようになった．
- `irori.followSymlinks` 設定（既定 `false`）．レイヤーの `roots` 配下を走査するときシンボリックリンクを辿る．循環参照を検出して打ち切り，深さ 64・エントリ 20 万の上限を設け，打ち切り時はユーザーに通知する．設定が無効なときも，スキップしたリンクの件数を警告として報告する．

### 修正

- `roots` を宣言しているのに走査先が 1 つも無いレイヤーについて，パネルが黙って空になる代わりに警告を 1 度だけ表示するようにした．


## 0.1.0

- アクティビティバーに irori コンテナを追加．スキーマ層 / オントロジー層 / ナレッジベース層 / Raw データ層 / その他 をそれぞれ独立したパネルとして表示
- `irori.layers` 設定でレイヤーを自由に定義可能（first-match-wins，最大 8 個）
- レイヤーの `roots` で Google Drive など Git 管理外のフォルダを走査・表示
- 標準エクスプローラーにレイヤーのバッジと色を付ける FileDecorationProvider
- コンパクトフォルダー表示，ファイル監視による自動更新，マルチルート対応
- ファイルのコンテキストメニュー（横に開く・エクスプローラーで表示・OS で表示・パスのコピー）
